import { update, remove, _beforeAll } from './enrollment-process.callbacks';

describe('/api/enrollmentprocess [TEST]', () => {
    beforeAll(_beforeAll)
    it.skip('should update step', update)
    // it.skip('should remove a enrollment process', remove)
})